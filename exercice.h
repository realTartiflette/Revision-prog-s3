#ifndef EXERCICE_H
#define EXERCICE_H

#include <stdio.h>
#include <stdlib.h>
#include "matrix.h"

unsigned long facto(unsigned long n);
unsigned long fibo(unsigned long n);
/*void bubble_sort(int* arr);
int_palindrome(int n);
matrix* mat_trans(matrix* m);
matrix* mat_mult(matrix* m);
void combine_string(char *s1, char *s2, char *s);*/

#endif


